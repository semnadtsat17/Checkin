/**
 * useOrgSettings — convenience re-export.
 * Import from here or directly from context; both work identically.
 *
 * Usage:
 *   const { mode, requireManagerApproval, superAdminEnabled } = useOrgSettings();
 */
export { useOrgSettings } from '../context/OrgSettingsContext';
